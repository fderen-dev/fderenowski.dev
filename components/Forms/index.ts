import { Input } from './controls/Input';
import { TextArea } from './controls/TextArea';
import { FormGroup } from './elements/FormGroup';
import { Form, FormStructure as FS } from "./Form";

const Forms = {
  Form,
  FormGroup,
  Input,
  TextArea
}

export default Forms;

export type FormStructure = FS;